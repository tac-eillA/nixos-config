#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
  exit 1
fi

command_string="$*"

if command -v kitty >/dev/null 2>&1; then
  exec kitty -e bash -lc "${command_string}; printf '\n'; read -n1 -rsp 'Press any key to close...'; printf '\n'"
fi

exec bash -lc "${command_string}"
